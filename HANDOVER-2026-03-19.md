# Handover — 2026-03-19

## Session Summary

Rahul picked up from a disconnected session that was redesigning MCQs and assessments for the PTM (Parent-Teacher Meeting) Training Program. This session ran the seeding script to push 59 updated MCQs and 30 assessment questions into the database, fixed a factual error about the "open lesson policy," replaced one assessment question entirely, seeded 10 teacher accounts into the program, and drafted a communication email for the teachers.

## What Was Done

- **Ran `update-mcqs.py`** — pushed 59 redesigned MCQs (across 5 sections) and 30 assessment questions into the Supabase database. The script deletes old exercises and re-inserts updated ones with 6 specific fixes applied.
- **Fixed "open lesson policy" question (Assessment Q23)** — the original question incorrectly said the open lesson policy was for parents to observe. Corrected: the open lesson policy is for *students* (any child can sit in on another group's lesson). Parents have separate observation opportunities.
- **Replaced Assessment Q23 entirely** — Rahul didn't like the observation question at all. Replaced it with a new question: a parent asking for help explaining the school's "no exams, no report cards" approach to skeptical in-laws/grandparents.
- **Seeded 10 teacher accounts** — created trainee records, enrolled them in the PTM program, and initialized progress for all 5 sections. These teachers will be auto-linked when they log in via Google OAuth with their @pepschoolv2.com accounts:
  - Archana Nair, Bharrathi Manogaran, Dewanshi Rawat, Dhanya R, Geetha Aravind, Naina N, Savitha N J, Shakun Gupta, Tharani Ranganathan, Trupthi Krishnan
- **Added `update-mcqs.py` to `.gitignore`** — contains Supabase service role key, so kept out of git.
- **Committed and pushed** PTM preparation guides (markdown + PDF) and the `.gitignore` update to `feature/training-engine`.
- **Drafted teacher communication email** — gentle, non-mandatory email suggesting teachers try the MCQ module as an easier alternative to reading the PDF.
- **Fixed "Avg Score: N/A" bug** — the admin dashboard score calculation only looked at voice exercises. Since PTM is all MCQs, score was always N/A. Fixed both `/api/manager` and `/api/manager/trainee/[id]` to show MCQ correct percentage when no voice exercises exist. Added `scoreType` field to distinguish display format.
- **Mobile responsiveness fixes** — comprehensive audit and fixes across all learner and admin pages: responsive padding, stacking layouts on small screens, `overflow-x-hidden` on body, `break-words` on long text, responsive grid columns, nav button stacking, etc.
- **Merged `feature/training-engine` to `main` and pushed** — but the responsive and score fixes appear to have been reverted during the merge (see Open Issues).

## Current State

- **PTM Training Program is fully seeded** — 59 MCQs, 30 assessment questions, 10 teachers enrolled. Teachers are actively using it.
- **Code is merged to `main` and pushed to production** — but some fixes were reverted during the merge (see Open Issues).
- **Dev server is stopped.**
- **The `update-mcqs.py` script** is in the project root but gitignored. It contains the service role key — don't commit it.

## Open Issues / Next Steps

### PRIORITY 1: Mobile Responsiveness is Broken on Production

All responsive CSS changes were made on `feature/training-engine` and merged to `main`, but **the changes appear to have been reverted during the merge** (likely by the linter/formatter during `git pull`). The admin dashboard and learner pages are broken on mobile — content overflows the right side of the screen, elements are clipped, nothing stacks properly.

**Files that need responsive fixes re-applied:**
- `src/app/layout.tsx` — needs `overflow-x-hidden` on body
- `src/app/login/page.tsx` — heading `text-2xl sm:text-3xl`
- `src/app/learn/page.tsx` — title + badge wrapping, responsive text
- `src/app/learn/[programSlug]/page.tsx` — progress section stacking, completion badge sizing
- `src/app/learn/[programSlug]/[sectionSlug]/page.tsx` — responsive padding, nav buttons stacking
- `src/app/learn/[programSlug]/assessment/page.tsx` — responsive option padding, results score sizing, action buttons stacking
- `src/components/MultipleChoice.tsx` — responsive padding on card and options, `break-words`
- `src/app/admin/dashboard/page.tsx` — responsive header, nav buttons, **trainee list rows overflowing on mobile** (the most visible issue — sections count and progress bars get clipped)
- `src/app/admin/users/[id]/page.tsx` — responsive stats grid, timeline, exercise indentation

**What to do:** Check if these files on `main` currently have the responsive classes. If not, re-apply the mobile fixes directly on `main` and push.

### PRIORITY 2: "Avg Score: N/A" Bug Fix May Have Been Reverted

The score calculation was fixed in two API routes to include MCQ scores (not just voice exercise scores). These changes may also have been reverted during the merge:
- `src/app/api/manager/route.ts` — dashboard API: score calculation + `scoreType` field
- `src/app/api/manager/trainee/[id]/route.ts` — detail API: score calculation + `scoreType`/`overallScoreType` fields
- `src/app/admin/dashboard/page.tsx` — display MCQ score as percentage vs voice as /5
- `src/app/admin/users/[id]/page.tsx` — same display logic

**What to do:** Check these files on `main`. If the score fix isn't there, re-apply it.

### PRIORITY 3: Scoring for Mixed Voice + MCQ Programs

Current logic: if a program has voice exercises, show avg voice score (out of 5). If no voice exercises, show MCQ percentage. But **what about programs that have BOTH voice and MCQ exercises?** Rahul raised this as something to think through — possibly show 2 separate scores. This needs design discussion before implementing.

### Other
- **Monitor teacher usage** — teachers are actively using the module, check admin dashboard for progress.
- **Email was drafted and shared** — Rahul has the email to send to teachers.

## Key Decisions Made

- **Open lesson policy = for students, not parents.** Students can observe other groups' lessons. Parents get separate observation opportunities. This is a factual correction from the school.
- **Assessment Q23 replaced entirely.** The parent-observation question was removed because parents already know about observation opportunities — it's not a realistic PTM question. Replaced with the "grandparents skeptical about no exams" question.
- **Script kept out of git** because it contains the Supabase service role key.
- **Email tone: suggestion, not mandate.** PTMs are imminent and teachers are busy, so the module is positioned as optional but recommended.

## Important Context

- **`update-mcqs.py`** is a one-shot seeding script. If you need to re-run it, it safely deletes and re-inserts everything (idempotent). It's in the project root but gitignored.
- **The 10 teachers don't have auth accounts yet.** Their trainee records exist and are enrolled. When they log in via Google OAuth with @pepschoolv2.com, the auth callback automatically links their auth user to the existing trainee record.
- **Program ID:** `660f131e-b2cb-453e-8056-4b26be03e7a4` (PTM Training Program)
- **Section IDs** are hardcoded in the script if you ever need them.
